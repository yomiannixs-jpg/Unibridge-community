# Stage 6I.2 — Room Click and Management Layout Fix

Fixes:
- Room cards are now full clickable links to `/rooms/:slug`.
- Room management cards no longer overlap.
- Manage page cards use a clean two-column responsive layout.
- Open/Edit/Reset/Delete buttons fit properly inside each card.

Push:
```powershell
git add .
git commit -m "Fix room click and management layout"
git push
```
